class User {
  constructor(username, password) {
    this.username = username;
    this.password = password;
    this.Preferences = new Preferences();
  }

  update_preferences(film, choice){

  }
}